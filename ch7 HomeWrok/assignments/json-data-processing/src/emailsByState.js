/**
 * `emailsByState()` takes an array of user profiles in the following format:
 * 
 * Check out profiles.json to see what they look like.
 * { 
 *   id: string;
 *   firstName: string;
 *   lastName: string;
 *   email: string;
 *   gender: string;
 *   state: string
 * }[]
 * 
 * The function will return an object where the key represents a state name,
 * and the corresponding value is an array of email addresses associated with
 * that state. If there are no email addresses for a specific state, the array
 * will be empty.
 * 
 * {
 *   "NJ": [
 *     "aeicke0@privacy.gov.au",
 *     "wgarza1y@seesaa.net",
 *     "tdenkin3f@hud.gov",
 *     ...
 *   ],
 *   ...
 * }
 */

export const emailsByState = profiles => {
  /////////////////////////
  // put your logic here //
  /////////////////////////
  return {};
};
